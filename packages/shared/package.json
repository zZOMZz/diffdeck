{
  "name": "@reviewdeck/shared",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc && esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm --platform=node"
  },
  "devDependencies": {
    "@reviewdeck/typescript-config": "workspace:*",
    "esbuild": "^0.28.0",
    "typescript": "~5.9.3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
